#!/usr/bin/env bash

set -euo pipefail

SCRIPT_PATH="$(python3 -c "import os; print(os.path.realpath('${BASH_SOURCE[0]}'))")"
ROOT_DIR="$(cd "$(dirname "$SCRIPT_PATH")/.." && pwd)"
exec "$ROOT_DIR/.venv/bin/python" "$ROOT_DIR/scripts/workspace.py" "$@"
