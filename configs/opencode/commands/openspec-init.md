---
description: Initialize Spec Kit for this repository
---

Initialize this project with `openspec init --tools codex,opencode`.
If `openspec` is not installed, suggest `npm install -g @fission-ai/openspec@latest`.
